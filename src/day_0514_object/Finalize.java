package day_0514_object;

public class Finalize {
	/**
	 * 쓰레기 수집기
	 * 참조하지 않는 배열이나 객체를 힙 영역에서 자동적으로 소멸시킨다
	 * - 쓰레기 수집기는 객체를 소멸하기 직전에 마지막으로 객체의 소멸가를 실행시킨다
	 * - 쓰레기 수집기는 메모리가 부족할때,CPU가 한가할 때에 JVM에 의해서 자동 실행된다
	 * - 쓰레기 객체를 순서대로 소멸시키지 안혹 무작위로 소멸시킨다
	 * - 쓰레기 객체를 전부 소멸시키는 것이 아니라 메모리의 상태를 보고 일부만 소멸시킨다
	 */
}
